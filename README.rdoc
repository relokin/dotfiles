= Nikos Nikoleris Dot Files

These are config files to set up a system the way I like it.


== Installation

  git clone git://github.com/protias/dotfiles ~/.dotfiles
  cd ~/.dotfiles
  git submodule init
  git submodule update
  ./install.py

